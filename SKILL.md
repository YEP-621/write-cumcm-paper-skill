---
name: write-cumcm-paper
description: 撰写、修改和验收中文全国大学生数学建模竞赛（CUMCM/国赛）XeLaTeX 论文。用于把已确认的赛题、建模报告、真实结果、代码和图表组织成高质量论文，改进已有 .tex/PDF 的论证与排版，或按 2026 格式、匿名性、AI 披露、参考文献中英文比例、数值一致性、算法说明和创新证据执行提交前审查。遇到结论越界、主观参数无来源或结果不足时必须阻断写作。仅支持 XeLaTeX，不用于 Word、Typst、美赛或其他竞赛。
---

# 国赛高质量 XeLaTeX 论文

先证明材料足以回答题目，再组织论文。不得用复杂模型、密集检验或漂亮排版掩盖证据缺口。

## 选择模式

- **写作**：从赛题、建模报告、结果、代码和图表创建完整工程。
- **修改**：读取现有 `.tex`、最新 PDF 和结果来源，保持真实模型与数值。
- **验收**：只审查不改稿，输出 `reports/PAPER_QUALITY_REPORT.md`。

## 按需读取参考

1. 始终读取 `references/format-and-compliance.md`。
2. 写作或重构正文时读取 `references/content-quality-gate.md` 和 `references/winning-paper-playbook.md`。
3. 涉及模型、算法、权重、阈值或创新时读取 `references/algorithm-and-innovation.md`。
4. 涉及 LaTeX、图表、公式或分页时读取 `references/visual-and-latex-style.md`。

## 输入要求

确认至少具有：

- 赛题原文、附件和字段说明；
- 每个子问题的变量、假设、模型、目标与约束；
- 可追溯到文件的真实结果、误差或检验；
- 可运行代码、图表数据和支撑材料；
- 真实且正文实际使用的参考文献；
- AI 使用事实和披露材料；本 skill 参与论文或代码处理时必须提供工具、用途、关键交互和人工修改记录。

缺少会改变结论的材料时停止，不生成“可提交正文”。不得编造结果、参数、权重、阈值、专家判断、实验、文献、创新或 AI 记录。

## 写作前硬门槛

先生成并填写：

- `reports/MODELING_WRITEABILITY_REPORT.md`
- `reports/CLAIM_EVIDENCE_MATRIX.md`

逐问审查题目要求、数据可识别性、主模型、结果来源、验证和结论边界。仅当可写性报告状态为 `PASS` 且矩阵无占位符时进入正文。

出现以下任一情况时，列出阻断项并询问用户：

- 用相关、一致或预测结果证明真实、因果、正确或错误；
- AHP 判断矩阵、专家权重、阈值、评价标准或一票否决规则没有来源；
- 结果报告、代码输出、图表和正文数值不一致；
- 必须重新建模或计算才能回答题目；
- 关键结果、基准或验证缺失。

写作 skill 不自行更换模型或运行未经授权的新实验。需要重算时交回建模/代码阶段。

## 初始化

```powershell
python scripts/init_paper.py <输出目录> --questions <子问题数>
```

生成 `document.tex`、`cumcmthesis.cls`、`texfile/`、`book.bib`、`support/` 和 `reports/`。初始化器只复制 skill 内部资产，不读取任何外部参考模板。

## 固定正文结构

不得随意合并、改名或调换以下结构：

1. 摘要；
2. 一、问题描述：1.1 问题背景，1.2 问题重述，并逐问重述；
3. 二、问题分析：2.1 问题一、2.2 问题二……，逐问写清疑点、可能方向、取舍和完整建模决策链；
4. 三、模型假设：只保留少量必要假设；
5. 四、定义与符号说明：先用指定引导句，再放三列三线符号表；
6. 五、模型的建立与求解：先写共用数据预处理，再按 5.1、5.2……逐问展开；每问固定包含总览、模型的建立、模型的求解、模型的计算结果、该问结果验证与解释；
7. 六、模型的评价：6.1 模型的优点，6.2 模型的缺点，6.3 模型的推广；
8. 参考文献另起一页；
9. 附录。

AI 使用说明可使用无编号标题放在参考文献前，不改变上述六个编号章节。赛题确有特殊结构需求时先说明原因并征得用户确认。

## 正文工作流

1. 依据主张—证据矩阵规划章节和图表。
2. 把“二、问题分析”写成完整的建模决策链：题目目标与输出、数据及可识别性、难点与风险、备选方案取舍、主模型选择、预处理与求解、验证方案、子问题依赖和结论边界。记录所有会影响模型的考虑，不写按时间流水账。
3. 每个子问题默认突出一个主模型；辅助模型必须用于明确的基线、对照、验证或风险检查。（如果建模确切用了多模型则忽略）
4. 每个子问题先写总览，再依次写模型的建立、模型的求解、模型的计算结果、该问结果验证与解释；不得把结果或验证混入算法介绍。
5. “三、模型假设”只保留会改变模型且在后文实际使用的少量假设，通常 3--6 条；逐条说明依据、影响和适用边界。
6. “四、定义与符号说明”使用宽度 `\CumcmTableWidth`（固定为 `0.92\textwidth`）的三线表，三列固定为“符号定义、符号说明、单位”，仅列跨章节或主模型的重要符号。原则上一行只写一个符号；仅当最终 PDF 页面空间确实不足时，才可把关系紧密的符号合并一行，并在表下注明原因。
7. 符号表未收录的符号必须在首次出现的公式后立即用“其中，\(x\) 为……”解释；索引、集合、上下标、向量/矩阵和单位均不得含糊。
8. 只有后文会通过“由公式（3-1）”“根据式（3-1）”等表述明确引用的公式才编号。这类公式必须使用行间环境 `CumcmReferencedEquation`，配合 `\label` 和后文的 `\eqref`，在最右侧显示“（章号-本章被引用公式序号）”。其余行内和行间公式一律不编号：行内直接使用 `\(...\)`，普通行间公式使用 `\[...\]`、`equation*` 或 `align*`。
9. 先写问题特定模型，再写算法。算法名不能替代目标函数、约束、参数和复现条件。
10. 用“现有不足 → 针对性改进 → 合理性 → 定量证据 → 适用边界”表达创新；证据不足时改称特点。
11. 最后写摘要，通常控制在 300--600 个汉字，逐问保留方法、关键结果和可信性证据，硬约束是不超过一页。“针对问题一”“针对问题二”等结构提示不得加粗；只选择性加粗真正重要的方法名、核心指标或关键数值，不得成段或密集加粗。关键词固定为 5 个，使用中文分号分隔，而且每个关键词必须含有中文，不得使用纯英文关键词。
12. 中文学术术语后不得附加括号内英文解释或缩写，例如写“主成分分析”，不写“主成分分析（PCA）”。没有通行中文名称的英文专名可独立出现，但关键词仍须含中文。
13. 所有正文表格即使内容简单也不得缩成窄表，统一尽量使用 `\CumcmTableWidth`（`0.92\textwidth`）横跨大部分版心；不得因内容少而缩小。
14. 同类图片统一宽度、字体、线宽、图例和清晰度；默认使用模板的 `\CumcmFigureWidth`，只有信息密度确有差异时才使用宽图或局部例外。
15. 除最后一页外，正文和附录各页不得留下约四分之一页以上的连续空白；通过调整浮动位置、表格断行、标题前空间和 `Needspace` 解决。摘要专页和按官方要求另起的参考文献页不受此条约束。
16. 参考文献必须另起一页；中文学术文献数量不得少于英文学术文献。AI、软件、题面和普通网站不计入比例。
17. 官方规定，完全未使用任何 AI 工具的参赛队须在参考文献之后声明“本参赛队未使用任何 AI 工具”。但使用本 skill 生成、改写、润色、审查论文或处理代码即构成使用 AI，必须在受辅助正文位置、参考文献和 `AI工具使用详情.pdf` 中一致披露，不得使用未使用声明。

## 编译与视觉复核

在工程根目录执行 XeLaTeX、BibTeX、XeLaTeX 两遍的完整链路。也可使用能执行同等链路的 `latexmk`。渲染最终 PDF 的全部页面，检查摘要、页码、标题、公式、图表、参考文献换页、附录和代码。

不得在用户提供的外部参考模板目录内编译、写入辅助文件或修改原文件。

## 自动验收

```powershell
python scripts/audit_paper.py document.tex `
  --pdf document.pdf `
  --ai-manifest support/ai-use.json `
  --results support/result-claims.json `
  --report reports/PAPER_QUALITY_REPORT.md
```

修复所有取消资格风险和硬错误。质量报告不能替代人工复核模型、文献真实性、代码和最终视觉效果。

验收必须额外确认：六个编号章节及逐问层级完整、问题分析逐问形成完整决策链、模型假设没有堆叠、定义与符号说明为 `0.92\textwidth` 三列表格且原则上一行一个符号、仅后文明确引用的公式按章编号、其余公式不编号，且所有公式符号已全局或就地解释、摘要结构提示未加粗、五个关键词均含中文、中文术语后无括号英文、同类图片尺寸一致、非末页无超过约四分之一页的连续空白，且最终 PDF 中图表清晰可读。

## 修改现有论文

1. 读取入口、全部章节、参考文献、最新 PDF、结果记录和代码。
2. 先生成可写性报告、主张—证据矩阵和基线验收报告。
3. 保留正确模型、真实数值、标签和引用；优先做最小而完整的修改。
4. 若发现模型不能支持结论，停止润色并报告建模缺口。
5. 每轮修改后重新编译、逐页渲染并验收。

## 验收分级

1. **取消资格风险**：官方格式、匿名、页数、大小、附录和 AI 披露。
2. **硬错误**：编译失败、参考文献分页或比例不合格、主观参数无来源、关键结果矛盾。
3. **质量警告**：摘要不完整、模型堆叠、算法说明不足、创新无证据、图表难读。
4. **优化建议**：不影响正确性但可改善可读性、篇幅或视觉一致性的事项。

每项问题必须指出证据位置、影响和可执行修复，不得只给总分。
